# 滑鼠破壞畫作：簡易開發計畫

## 1. 專案目標

製作一個可直接在瀏覽器開啟的單一 `畫作顔色亂混.html`。畫面顯示一幅完整畫作，滑鼠在畫作上移動時，附近的顏料會沿移動方向被推開並與周圍顏色混合。反覆移動後，原本的圖像會逐漸變形、模糊，形成被破壞的效果。

作品提供三種可切換的破壞手法：

- **Smear（抹開）**：顏料沿滑鼠方向被推開並混合，屬於塗抹混色的破壞。
- **RGB Split（色差）**：滑鼠掃過的區域會產生四種彩色數位破壞效果——RGB displacement（紅藍通道沿掃動方向錯位）、Scanline（掃描線變暗與整列水平錯位）、Pixel displacement（方塊像素位移場）、Datamosh（殘影拖尾、macroblock 錯置與色階壓縮）；破壞會固定累積在畫布上，可反覆疊加。
- **Glitch（數位破壞）**：每次點擊圖片，在點擊處產生一次隨機的數位損壞——透過圖片內容的局部切片錯位、拉伸、複製拖影、水平或垂直位移形成細長影像碎片與不規則錯位；受損區域固定為扁長形（寬遠大於高）且範圍壓小，破壞程度與形態每次隨機（有時輕微、有時明顯錯位），保留原圖色彩與風格。效果可超出圖片邊緣蔓延到框外，固定累積，多次點擊逐漸形成被數位故障破壞的 artwork。

畫面頂部（手機為底部）可隨時切換手法；重置按鈕會還原成完整原畫。

## 2. 互動方式

- 滑鼠移動：推動附近顏料，並留下混色筆觸。
- 手法切換：頂部分段按鈕切換 Smear（抹開）、RGB Split（色差）與 Glitch（數位破壞）；長按旋渦僅在 Smear 生效，RGB Split 與 Glitch 模式游標會變成虛線方框。
- RGB Split 模式：滑鼠或手指掃過畫作即觸發，不需按住；破壞沿掃動路徑依序出現並固定累積在畫布上。
- Glitch 模式：每次點擊圖片觸發一次隨機數位損壞，拖曳不產生效果；每次點擊的強度與形態都不同。
- 上傳圖片：可透過右上角上傳按鈕、直接拖放圖片到頁面，或剪貼簿貼上（Ctrl+V）載入自己的圖片；載入後自動裁除四周均勻白邊，成為新的互動畫布。
- 原地長按：按住約 500ms 後，顏色會以按壓點為中心快速旋轉混合並逐步擴張。
- 移動方向：決定顏料被推向哪裡。
- 移動速度：只輕微影響位移距離，避免效果過於複雜。
- 反覆經過：顏色持續混合，畫作逐漸失去原貌。
- 向畫框外移動：已取到的顏料會被繼續拖入白色背景。
- 觸控裝置：手指滑動使用相同效果。
- 重置按鈕：恢復最初的完整畫作。

長按時若移動超過小範圍、放開或離開畫面，旋渦會立即停止。

## 3. 視覺設計

### 畫作

- 使用使用者提供的 `克羅姆-吻.jpg` 作為畫作素材。
- 裁掉原圖四周白邊，只保留完整畫框與內部構圖。
- 畫框置於全視窗中央，桌面版最高為視窗高度的 78%，手機則依寬度等比例縮放。

### 介面

- 網頁使用純白背景，以畫框作為唯一視覺焦點。
- 左上角顯示項目名稱「破壞藝術 Destruction Canva」。
- 開始時顯示短提示「移動滑鼠，攪動畫面」，互動後淡出。
- 右上角由左至右依序為上傳、匯出、重置三個圖示按鈕。
- 拖放圖片到頁面時顯示虛線覆蓋層提示「放開滑鼠，載入圖片」。
- 匯出按鈕可選擇 PNG（無損）或 JPG（檔案較小），匯出當下的畫面成果並自動下載，完成後顯示短暫提示。
- 顯示簡單的圓形筆刷游標，表示作用範圍。

## 4. 技術方案

### 檔案結構（2026-09-16 工程化重構）

- 入口為 `index.html`（只含 HTML 結構），載入同資料夾的畫作圖片與模組化資源。
- 樣式集中在 `src/styles/main.css`。
- JavaScript 使用原生 ES Modules，不使用框架、套件、CDN 或遠端素材：

```
src/js/
  canvases.js      ← DOM/canvas/context 實例集中管理
  state.js         ← 可變狀態（模式、游標、視圖、各手法狀態）
  display.js       ← 版面縮放、重置、白邊裁切
  ui.js            ← toast、匯出、上傳/拖放/貼上、手法切換
  main.js          ← 進入點：事件綁定、動畫迴圈、啟動
  effects/
    smear.js       ← 抹開 + 旋渦
    glitch.js      ← RGB Split + 點擊爆發
    melt.js        ← 融化水流
```

- 舊的單檔 `畫作顔色亂混.html` 保留作為歷史參考，不再維護。
- 因使用 ES Modules，需透過本機 HTTP server 開啟（不能 file:// 直接開）。

### Canvas 實作

- 使用一個 HTML Canvas 繪製畫作與互動結果。
- 每次指標移動時，取得目前位置與上一個位置之間的方向和距離。
- 從游標周圍擷取一小塊圓形畫面。
- 將擷取內容沿滑鼠方向稍微錯位，以兩層清晰位移維持拖曳感。
- 額外疊上一層低透明度柔化取樣，使相鄰顏色產生中間色而不會整體失焦。
- 在移動路徑上補足數個取樣點，避免快速移動時筆觸斷裂。
- 每次都從已變形的畫面再次取樣，使顏色自然累積、拖曳和混合。
- 使用動畫迴圈持續旋轉長按位置的圓形取樣，並以低頻率疊色形成局部旋渦。

### RGB Split 與 Glitch 實作

- 沿掃動路徑排入取樣點（間距隨筆刷半徑調整），每幀自佇列取出固定數量處理，掃過處依序爆裂。
- 取樣點能量取自平滑後的滑鼠速度（px/ms），直接決定該區域的破壞強度。
- 每個取樣點以離屏畫布擷取方形區域，處理後以柔邊遮罩烘焙回主畫布。
- RGB Split（彩色）四種效果：
  - RGB displacement：紅、綠、藍三通道沿掃動方向前後錯位取樣。
  - Scanline：每 N 列變暗，部分整列水平錯位並平滑銜接。
  - Pixel displacement：粗方格位移場隨機推擠方塊。
  - Datamosh：殘影拖尾（lighter 疊加）、隨機區塊重複貼上與色階壓縮。
- Glitch（點擊式隨機數位損壞）：
  - 每次點擊隨機產生強度（0.3～1）、切片數量與厚度；受損區域固定為扁長形（寬約畫作 24%～52%，高約畫作 3.5%～11%），不會出現大面積破壞。
  - 水平切片錯位＋隨機拉伸＋局部垂直位移，部分切片帶半透明拖影。
  - 隨機出現垂直切片錯位；強度高時產生大幅位移的細長影像碎片。
  - 受損區域撒上稀疏的細小彩色噪點（密度隨強度並每次隨機，上限壓低只做點綴）與偶爾出現的少量彩色橫向雜訊條；整體色調不受影響。

### 基本效能處理

- 使用 Pointer Events 同時支援滑鼠和觸控。
- 限制筆刷尺寸與每次移動的取樣數量。
- 限制高 DPI 畫布倍率，避免像素處理過重。
- 視窗改變尺寸時重新建立畫作。

## 5. 實作步驟

1. 建立全視窗 Canvas 與簡潔背景。
2. 將上傳畫作依視窗比例繪製到 Canvas。
3. 追蹤指標位置、移動方向與速度。
4. 完成圓形筆刷的像素擷取、清晰位移與柔化混色。
5. 加入路徑補點，讓筆觸保持連續。
6. 加入提示文字、筆刷游標與重置按鈕。
7. 測試桌面滑鼠與手機觸控。

## 6. 驗收清單

- [ ] 直接開啟 `畫作顔色亂混.html` 即可使用。
- [ ] 畫面只使用顏料位移混合作為破壞效果。
- [ ] 素材四周白邊已裁除，但顏料能從畫框自然延伸到外部白色背景。
- [ ] 顏料會沿滑鼠或手指移動方向被推動。
- [ ] 反覆經過同一區域時，顏色會持續混合並改變原畫。
- [ ] 原地長按 500ms 後會持續產生局部旋渦，放開或移動後停止。
- [ ] 快速移動時不會出現明顯的斷裂筆觸。
- [ ] 重置按鈕能完整恢復初始畫面。
- [ ] 匯出按鈕可將目前畫面下載為 PNG 或 JPG，檔名含時間戳記。
- [ ] 可透過按鈕、拖放或貼上載入自己的圖片，白邊自動裁除並可立即互動。
- [ ] 可在 Smear、RGB Split 與 Glitch 之間即時切換，提示文字與游標樣式隨手法更新。
- [ ] RGB Split 模式掃過畫作會產生彩色 RGB displacement、Scanline、Pixel displacement 與 Datamosh 效果。
- [ ] Glitch 模式每次點擊在點擊處產生一次隨機數位損壞，每次效果不同且保留原圖色彩。
- [ ] RGB Split 與 Glitch 破壞固定累積在畫布上，重置可完整還原。
- [ ] 桌面與手機畫面皆能正常操作。
- [ ] 長時間互動時仍保持流暢。

## 7. 最終交付物

- `PLAN.md`：簡化後的互動與技術計畫。
- `畫作顔色亂混.html`：離線可用的互動成品。
- `克羅姆-吻.jpg`：使用者提供的畫作素材，與 HTML 放在同一資料夾。